// The Construct's security headers live in the framework package so every game
// built on it serves the same ones; this re-export keeps the local paths working.
export * from '@crowdedkingdoms/construct/node/security-headers';
